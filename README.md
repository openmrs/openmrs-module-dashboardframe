Adds a tab with an embedded iframe to the OpenMRS patient dashboard

Tab Name property key: patient.iframe.tab_name
Website Url property key: patient.iframe.URL

where to change global values:
http://localhost:8080/openmrs/admin/maintenance/globalProps.form

NOTE:
website url MUST have {patientId} for place patientId
